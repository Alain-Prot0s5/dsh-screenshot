# Changelog / 更新日志

## v1.0.4 (2026-08-24)

**修复 / Fixes**
- **截图粘贴不再改变 DSH Desktop 窗口大小/位置**：监听器 `Set-DshForeground` 曾**无条件 `ShowWindow(SW_RESTORE)`**，会把**最大化/全屏**窗口还原成普通小窗；现改为**仅在窗口最小化时才恢复**——最大化/全屏在截图粘贴时保持不变（只把窗口带到前台，不改大小位置）。

## v1.0.3 (2026-08-23)

**文档 / Docs**
- README 增加 **Screenshots / 截图** 区（4 张：新对话首页 / 截图相机工具栏 / 已复制到剪贴板 / 插件设置卡片），图片位于 `assets/`
- npm 包 `files` 清单加入 `assets`，截图随包发布

## v1.0.2 (2026-08-22)

**兼容 / Compatibility**
- 支持 **DSH Desktop 2.0.2**（延续 v1.0.1 的包身份兼容，客户端/监听器在 2.0.2 下全部功能正常）

**修复 / Fixes**
- **"假粘贴"问题全面加固**（截图后 Ctrl+V 落空但日志记成功）：
  - 粘贴前**前台校验**：确认 DSH 窗口真在前台，失败重试 3 次；未确认则跳过粘贴，避免误粘到其它窗口
  - **剪贴板钉住**：粘贴前主动触发 SnippingTool 延迟渲染，避免数据未渲染完就粘贴
  - **客户端焦点守护**：截图遮罩关闭时自动聚焦输入框（焦点在输入框外也能正确粘贴）
  - **后台跳转粘贴修复**：pending 状态保持到粘贴完成，DSH 在后台时"跳到前台并粘贴"功能恢复
- 监听器日志记录触发来源（`触发截图（热键）/（按钮）`），便于排查

**样式 / Style**
- 相机按钮改为**正圆形**，与输入行其它图标按钮（模型选择、Workspace Write 等）观感一致

## v1.0.1 (2026-08-22)

**修复 / Fixes**
- **DSH Desktop ≥ 2.0.2 包身份校验兼容**：包名统一为完整 scoped 名 `@alain-prot0s5/dsh-screenshot`（修复 2.0.2 启动报 `profile package identity is invalid`）
- **相机按钮点击导致输入框失焦**（按钮触发"假粘贴"）：按钮 `tabIndex=-1` + 点击后恢复输入框焦点
- package.json 补充 `author` 字段

## v1.0.0 (2026-08-22)

**首个稳定版 / First stable release**
- 相机按钮 / 全局热键（默认 Alt+A）→ 系统截图（`ms-screenclip:`）→ 自动粘贴进输入框
- 设置页：快捷键 / 自动粘贴 / 后台跳转粘贴，即时生效
- 监听器随 DSH Desktop 生命周期启停（`-ExitWhenDshGone` + 看门狗）
- 非阻塞剪贴板等待、单飞防抖、取消检测、SnippingTool 卡死自愈
